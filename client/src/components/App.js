import React from 'react';
import axios from 'axios';

const App = props => {

    const login = () => {
        axios.
    }

    return(
        <div>
            <h1>App</h1>
            <button onClick={() => login()}>Login</button>
        </div>
    )
}

export default App;